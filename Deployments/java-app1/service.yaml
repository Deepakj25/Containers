apiVersion: v1
kind: Service
metadata:
  name: my-java-app-service
spec:
  type: LoadBalancer
  selector:
    app: my-java-app
  ports:
    - protocol: TCP
      port: 80           # Public-facing port
      targetPort: 8080   # Your container's internal port
